#include<iostream>
using namespace std;
void solve(int *arr,int n,int indx,int out[],int tn){
    int i=0;
    int temp[4];

      if(indx>n){
        for(int i=0;i<tn;i++){
       out[i++] = temp[i];
    }
    // this is paoer set set algo how to solve power or derive power st algp
    now it is like another to make one of the anor
    return ;
    }
} int i=0;
    int temp[4];

      if(indx>n){
        for(int i=0;i<tn;i++){
       out[i++] = temp[i];
    }
    // this is paoer set set algo how to solve power or derive power st algp
    now it is like another to make one of the anor
    return ;
    }
}
int main(){

    int n=3;
    int arr[3]={3,1,0};
    int indx=0;
    int out[8];
     solve(arr,3,indx,out,8);


  
    return 0;
}